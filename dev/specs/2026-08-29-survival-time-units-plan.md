# Settable Survival Time Units — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stop `plot.gg_variable()` and `plot.gg_rfsrc()` asserting a survival time unit
the package was never told, and give callers a `time_units` argument to supply the
one their fit actually uses.

**Architecture:** Three internal helpers in `R/utils.R` own validation and label
composition; the two exported plot methods each gain one `time_units = NULL` formal
and call the helpers at their three label sites. No new dependency, no change to any
returned object's class, elements or column names.

**Tech Stack:** R, ggplot2 4.0.3, roxygen2, testthat 3.3.2 (edition 2), vdiffr 1.0.9, lintr.
Label assertions use the `p$labels$y` idiom already established in
`test_gg_shap.R:324` and `test_gg_partial.R:465`, with `expect_equal` to match
those neighbours. `testthat::snapshot_accept()` is available and is the accept
route for the vdiffr baselines.

**Design spec:** `dev/specs/2026-08-29-survival-time-units-design.md`

## Global Constraints

- **Never push to `main`.** Work on `claude/sweet-williams-2c2479`, open a PR, stop.
  The maintainer merges.
- **Every suite run uses both variables:**
  `NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::test()'`.
  A run with `VDIFFR_RUN_TESTS` unset deletes all 49 vdiffr baselines as "unused".
  Run `git status` before and after every suite run; a wave of deleted `_snaps/`
  files means the variable was missing, not a change to commit.
- **`devtools::document()` runs before lint and tests, every time.** `man/` and
  `NAMESPACE` are generated.
- **No version bump.** `DESCRIPTION` and the `NEWS.md` `Version:` line both stay at
  `4.0.0`. This lands as a bullet under the existing
  `ggRandomForests v4.0.0 (development)` heading.
- **Lint must be 0.** `.lintr` sets `line_length_linter(120)`; `object_usage_linter`
  is disabled, so lint will NOT catch an undefined symbol — do not treat green lint
  as proof the code runs.
- **testthat edition 2.** Do not assume 3rd-edition semantics.
- **Every `test_that()` block that touches the RNG calls `set.seed()` inside that
  block.** `test_determinism.R` parses the suite and fails on a new unseeded block.
- **Do not touch the `time_labels` formal** at `R/plot.gg_variable.R:137`. It is a
  pre-existing dead parameter, out of scope by design.
- **Regenerate vdiffr baselines LAST**, in Task 4 only. Never run a blanket
  `git checkout -- tests/testthat/_snaps/` — it reverts intended regenerations along
  with any pruning.

## File Structure

| File | Responsibility | Task |
|---|---|---|
| `R/utils.R` | Add 3 internal helpers: validation + the two label shapes | 1 |
| `tests/testthat/test_utils.R` | Unit-test the helpers in isolation | 1 |
| `R/plot.gg_variable.R` | `time_units` formal + roxygen; 2 survival label sites | 2 |
| `tests/testthat/test_gg_variable.R` | Label assertions, both panel paths | 2 |
| `R/plot.gg_rfsrc.R` | `time_units` formal + roxygen; 1 survival label site | 3 |
| `tests/testthat/test_gg_rfsrc.R` | Label assertions, survival branch | 3 |
| `NEWS.md` | One bullet under the 4.0.0 (development) heading | 4 |
| `man/`, `NAMESPACE` | Regenerated by `devtools::document()` | 4 |
| `tests/testthat/_snaps/snapshots/gg-rfsrc-survival-*.svg` | 2 baselines regenerated | 4 |

`autoplot.gg_variable()` and `autoplot.gg_rfsrc()` in `R/autoplot_methods.R` are thin
`plot(object, ...)` forwarders and need **no** edit — `time_units` reaches them free.

---

### Task 1: Internal helpers for time-unit validation and label composition

**Files:**
- Modify: `R/utils.R` (append at end, after `.escape_regex()` which ends at line 222)
- Test: `tests/testthat/test_utils.R`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces, all internal (dot-prefixed, not exported, no man page):
  - `.check_time_units(time_units)` → `NULL` or a length-1 character. Errors otherwise.
  - `.survival_at_label(time_value, time_units)` → character(1), e.g.
    `"Survival at 1191"` or `"Survival at 1191 days"`.
  - `.time_axis_label(time_units)` → character(1), `"time"` or `"time (days)"`.

Tasks 2 and 3 call these by exactly these names and signatures.

- [ ] **Step 1: Write the failing tests**

Check whether `tests/testthat/test_utils.R` already exists:

```bash
ls tests/testthat/test_utils.R
```

If it exists, append the block below to it. If it does not, create it with the block
below as its entire contents. Either way the block is identical — these helpers do
not touch the RNG, so no `set.seed()` is needed.

```r
test_that(".check_time_units accepts NULL and a single string, rejects the rest", {
  expect_null(.check_time_units(NULL))
  expect_identical(.check_time_units("days"), "days")

  expect_error(.check_time_units(1), "single non-empty character")
  expect_error(.check_time_units(c("days", "years")), "single non-empty character")
  expect_error(.check_time_units(NA_character_), "single non-empty character")
  expect_error(.check_time_units(""), "single non-empty character")
})

test_that(".survival_at_label omits the unit unless one is given", {
  expect_identical(.survival_at_label(1191, NULL), "Survival at 1191")
  expect_identical(.survival_at_label(1191, "days"), "Survival at 1191 days")

  ## gg_dta$time is a factor; the level label must be used, not the integer code.
  tf <- factor("1191", levels = c("1191", "2000"))
  expect_identical(.survival_at_label(tf[1], NULL), "Survival at 1191")
})

test_that(".time_axis_label omits the unit unless one is given", {
  expect_identical(.time_axis_label(NULL), "time")
  expect_identical(.time_axis_label("days"), "time (days)")
})
```

- [ ] **Step 2: Run the tests to verify they fail**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_utils.R")'
```

Expected: FAIL, `could not find function ".check_time_units"`.

- [ ] **Step 3: Write the implementation**

Append to the end of `R/utils.R`:

```r
# --------------------------------------------------------------------------- #
# Survival time-unit labelling.  Nothing on an rfsrc fit records what the time
# column was measured in -- randomForestSRC carries no units metadata, and
# gg_variable() builds its 'time' column from rf$time.interest -- so the package
# cannot derive the unit and must not assert one.  The default prints no unit,
# which is always correct; a caller who knows the unit supplies it.
#' @keywords internal
.check_time_units <- function(time_units) {
  if (is.null(time_units)) {
    return(NULL)
  }
  if (!is.character(time_units) || length(time_units) != 1L ||
        is.na(time_units) || !nzchar(time_units)) {
    stop("'time_units' must be NULL or a single non-empty character string.",
         call. = FALSE)
  }
  time_units
}

# Survival y-axis title: "Survival at 1191" / "Survival at 1191 days".
# time_value is typically a factor level, so as.character() takes the label
# rather than the integer code.
#' @keywords internal
.survival_at_label <- function(time_value, time_units) {
  paste(c("Survival at", as.character(time_value), time_units), collapse = " ")
}

# Survival x-axis title: "time" / "time (days)".
#' @keywords internal
.time_axis_label <- function(time_units) {
  if (is.null(time_units)) {
    "time"
  } else {
    paste0("time (", time_units, ")")
  }
}
```

- [ ] **Step 4: Run the tests to verify they pass**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_utils.R")'
```

Expected: PASS, 0 failures. (`SS` output means the env vars were dropped — rerun.)

- [ ] **Step 5: Lint**

```bash
Rscript -e 'lintr::lint_package()'
```

Expected: no output (0 lints).

- [ ] **Step 6: Commit**

```bash
git add R/utils.R tests/testthat/test_utils.R
git commit -m "feat: internal helpers for settable survival time units"
```

---

### Task 2: Wire time_units into plot.gg_variable()

**Files:**
- Modify: `R/plot.gg_variable.R` — roxygen near line 32, signature at 134-143,
  label sites at 345 and 524
- Test: `tests/testthat/test_gg_variable.R`

**Interfaces:**
- Consumes: `.check_time_units()`, `.survival_at_label()` from Task 1.
- Produces: `plot.gg_variable(..., labels = NULL, time_units = NULL, ...)`.

**Why `time_units` must be a formal, not left to `...`:** line 158 is
`gg_dta <- gg_variable(x, ...)`. The method forwards `...` into `gg_variable()`, so a
loosely-passed `time_units` would arrive there as an unknown argument. Declaring it
captures it before the forward.

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test_gg_variable.R`:

```r
test_that("plot.gg_variable survival panel: no unit by default, unit when given", {
  skip_on_cran()
  data(veteran, package = "randomForestSRC")
  set.seed(42)
  rfsrc_veteran <- randomForestSRC::rfsrc(
    Surv(time, status) ~ .,
    data = veteran,
    ntree = 50,
    nsplit = 5
  )

  gg_dta <- gg_variable(rfsrc_veteran, time = 90)

  gg_plt <- plot(gg_dta, xvar = c("age", "diagtime"), panel = TRUE)
  expect_equal(gg_plt$labels$y, "Survival at 90")
  expect_false(grepl("year", gg_plt$labels$y, fixed = TRUE))

  gg_units <- plot(gg_dta, xvar = c("age", "diagtime"), panel = TRUE,
                   time_units = "days")
  expect_equal(gg_units$labels$y, "Survival at 90 days")
})

test_that("plot.gg_variable survival individual: no unit by default, unit when given", {
  skip_on_cran()
  data(veteran, package = "randomForestSRC")
  set.seed(42)
  rfsrc_veteran <- randomForestSRC::rfsrc(
    Surv(time, status) ~ .,
    data = veteran,
    ntree = 50,
    nsplit = 5
  )

  gg_dta <- gg_variable(rfsrc_veteran, time = 90)

  gg_plt <- plot(gg_dta, xvar = "age")
  expect_equal(gg_plt$labels$y, "Survival at 90")
  expect_false(grepl("year", gg_plt$labels$y, fixed = TRUE))

  gg_units <- plot(gg_dta, xvar = "age", time_units = "days")
  expect_equal(gg_units$labels$y, "Survival at 90 days")
})

test_that("plot.gg_variable rejects a malformed time_units", {
  skip_on_cran()
  data(veteran, package = "randomForestSRC")
  set.seed(42)
  rfsrc_veteran <- randomForestSRC::rfsrc(
    Surv(time, status) ~ .,
    data = veteran,
    ntree = 50,
    nsplit = 5
  )

  gg_dta <- gg_variable(rfsrc_veteran, time = 90)
  expect_error(plot(gg_dta, xvar = "age", time_units = c("a", "b")),
               "single non-empty character")
})

test_that("plot.gg_variable regression y label is untouched by the units change", {
  skip_on_cran()
  set.seed(42)
  rfsrc_air <- randomForestSRC::rfsrc(
    Ozone ~ .,
    data = stats::na.omit(airquality),
    ntree = 50,
    nsplit = 5
  )

  gg_dta <- gg_variable(rfsrc_air)
  gg_plt <- plot(gg_dta, xvar = "Temp")
  expect_false(grepl("Survival at", gg_plt$labels$y, fixed = TRUE))
})
```

- [ ] **Step 2: Run the tests to verify they fail**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_gg_variable.R")'
```

Expected: FAIL. The default-label assertions fail with
`"Survival at 90 year" not identical to "Survival at 90"`; the `time_units` calls fail
with an unused-argument error from `gg_variable()`.

- [ ] **Step 3: Add the roxygen `@param`**

In `R/plot.gg_variable.R`, insert a new block immediately after the `@param labels`
block (which ends `...Defaults to \code{NULL} (raw names).`) and immediately before
`#' @param ... arguments passed to the \code{ggplot2} functions.`:

```r
#' @param time_units Optional name of the time unit the forest was fit in, used
#'   only in the survival y axis title. \code{plot(gg_dta, time = 1191)} titles
#'   the axis \code{"Survival at 1191"}; \code{time_units = "days"} makes that
#'   \code{"Survival at 1191 days"}. Nothing on an
#'   \code{\link[randomForestSRC]{rfsrc}} object records the unit, so the
#'   package cannot infer it. Defaults to \code{NULL} (no unit printed).
```

- [ ] **Step 4: Add the formal**

Change the signature at lines 134-143 from:

```r
plot.gg_variable <- function(x, # nolint: cyclocomp_linter
                             xvar,
                             time,
                             time_labels,
                             panel = FALSE,
                             oob = TRUE,
                             points = TRUE,
                             smooth = TRUE,
                             labels = NULL,
                             ...) {
  gg_dta <- x
```

to:

```r
plot.gg_variable <- function(x, # nolint: cyclocomp_linter
                             xvar,
                             time,
                             time_labels,
                             panel = FALSE,
                             oob = TRUE,
                             points = TRUE,
                             smooth = TRUE,
                             labels = NULL,
                             time_units = NULL,
                             ...) {
  gg_dta <- x
  time_units <- .check_time_units(time_units)
```

- [ ] **Step 5: Replace the panel-branch label site**

At what is currently line 345, change:

```r
          labs(
            x = "",
            y = paste("Survival at", gg_dta$time[1], "year")
          )
```

to:

```r
          labs(
            x = "",
            y = .survival_at_label(gg_dta$time[1], time_units)
          )
```

- [ ] **Step 6: Replace the individual-branch label site**

At what is currently line 524, change:

```r
            ggplot2::labs(
              x = h_display,
              y = paste("Survival at", gg_dta$time[1], "year")
            )
```

to:

```r
            ggplot2::labs(
              x = h_display,
              y = .survival_at_label(gg_dta$time[1], time_units)
            )
```

- [ ] **Step 7: Confirm both sites are gone**

```bash
grep -n 'Survival at' R/plot.gg_variable.R
```

Expected: two hits, both reading `y = .survival_at_label(gg_dta$time[1], time_units)`.
No hit containing the literal `"year"`.

- [ ] **Step 8: Run the tests to verify they pass**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_gg_variable.R")'
```

Expected: PASS, 0 failures.

- [ ] **Step 9: Lint**

```bash
Rscript -e 'lintr::lint_package()'
```

Expected: no output (0 lints).

- [ ] **Step 10: Commit**

```bash
git add R/plot.gg_variable.R tests/testthat/test_gg_variable.R
git commit -m "fix: stop plot.gg_variable() asserting a 'year' survival time unit"
```

---

### Task 3: Wire time_units into plot.gg_rfsrc()

**Files:**
- Modify: `R/plot.gg_rfsrc.R` — roxygen near line 36-40, signature at 160,
  label site at 290
- Test: `tests/testthat/test_gg_rfsrc.R`

**Interfaces:**
- Consumes: `.check_time_units()`, `.time_axis_label()` from Task 1.
- Produces: `plot.gg_rfsrc(x, notch = TRUE, time_units = NULL, ...)`.

**Expect two snapshot failures after this task.** `gg-rfsrc-survival-no-ci.svg` and
`gg-rfsrc-survival-bootstrap-ci.svg` both contain the string `time (years)` and will
go red. That is correct and intended. They are regenerated in Task 4, last, per
`AGENTS.md`. Do NOT regenerate them here, and do NOT revert this change to make them
pass.

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test_gg_rfsrc.R`:

```r
test_that("plot.gg_rfsrc survival: no unit by default, unit when given", {
  data(veteran, package = "randomForestSRC")
  set.seed(42)
  rfsrc_veteran <- randomForestSRC::rfsrc(
    Surv(time, status) ~ .,
    data = veteran,
    ntree = 50,
    nsplit = 5,
    forest = TRUE,
    save.memory = TRUE
  )

  gg_dta <- gg_rfsrc(rfsrc_veteran)

  gg_plt <- plot(gg_dta)
  expect_equal(gg_plt$labels$x, "time")
  expect_false(grepl("year", gg_plt$labels$x, fixed = TRUE))
  expect_equal(gg_plt$labels$y, "Survival (%)")

  gg_units <- plot(gg_dta, time_units = "days")
  expect_equal(gg_units$labels$x, "time (days)")
})

test_that("plot.gg_rfsrc rejects a malformed time_units", {
  data(veteran, package = "randomForestSRC")
  set.seed(42)
  rfsrc_veteran <- randomForestSRC::rfsrc(
    Surv(time, status) ~ .,
    data = veteran,
    ntree = 50,
    nsplit = 5,
    forest = TRUE,
    save.memory = TRUE
  )

  gg_dta <- gg_rfsrc(rfsrc_veteran)
  expect_error(plot(gg_dta, time_units = 7), "single non-empty character")
})
```

- [ ] **Step 2: Run the tests to verify they fail**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_gg_rfsrc.R")'
```

Expected: FAIL with `"time (years)" not identical to "time"`, and an
unused-argument error for `time_units`.

- [ ] **Step 3: Add the roxygen `@param`**

In `R/plot.gg_rfsrc.R`, insert immediately before the existing
`#' @param ... Additional arguments forwarded to the underlying` block (line 40):

```r
#' @param time_units Optional name of the time unit the forest was fit in, used
#'   only in the survival x axis title. The default titles the axis
#'   \code{"time"}; \code{time_units = "days"} makes that \code{"time (days)"}.
#'   Nothing on an \code{\link[randomForestSRC]{rfsrc}} object records the unit,
#'   so the package cannot infer it. Defaults to \code{NULL} (no unit printed).
```

- [ ] **Step 4: Add the formal**

Change line 160 and the two lines under it from:

```r
plot.gg_rfsrc <- function(x, notch = TRUE, ...) {
  gg_dta <- x
```

to:

```r
plot.gg_rfsrc <- function(x, notch = TRUE, time_units = NULL, ...) {
  gg_dta <- x
  time_units <- .check_time_units(time_units)
```

Declaring it as a formal also keeps it out of `arg_set <- list(...)` on line 165,
which is spread into `geom_step()` and would otherwise error on an unknown aesthetic.

- [ ] **Step 5: Replace the label site**

At what is currently line 290, change:

```r
      ggplot2::labs(x = "time (years)", y = "Survival (%)")
```

to:

```r
      ggplot2::labs(x = .time_axis_label(time_units), y = "Survival (%)")
```

- [ ] **Step 6: Confirm no hardcoded unit remains anywhere in R/**

```bash
grep -rn 'time (years)\|"year")' R/
```

Expected: no output.

- [ ] **Step 7: Run the tests to verify they pass**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::load_all(); testthat::test_file("tests/testthat/test_gg_rfsrc.R")'
```

Expected: PASS, 0 failures. (`test_gg_rfsrc.R` contains no vdiffr calls, so the two
stale baselines do not surface here — they surface in the full suite in Task 4.)

- [ ] **Step 8: Lint**

```bash
Rscript -e 'lintr::lint_package()'
```

Expected: no output (0 lints).

- [ ] **Step 9: Commit**

```bash
git add R/plot.gg_rfsrc.R tests/testthat/test_gg_rfsrc.R
git commit -m "fix: stop plot.gg_rfsrc() asserting a 'years' survival time unit"
```

---

### Task 4: Document, NEWS, full gate, regenerate baselines

**Files:**
- Modify: `NEWS.md` (bullet under the existing 4.0.0 heading)
- Regenerate: `man/`, `NAMESPACE` via `devtools::document()`
- Regenerate: `tests/testthat/_snaps/snapshots/gg-rfsrc-survival-no-ci.svg`,
  `tests/testthat/_snaps/snapshots/gg-rfsrc-survival-bootstrap-ci.svg`

**Interfaces:**
- Consumes: the finished `time_units` formals from Tasks 2 and 3.
- Produces: a branch ready for PR.

- [ ] **Step 1: Add the NEWS bullet**

In `NEWS.md`, under the `ggRandomForests v4.0.0 (development)` heading, add:

```markdown
* `plot.gg_variable()` and `plot.gg_rfsrc()` no longer hard-code a "year" time
  unit in survival axis titles. The unit was never derived from the data, so a
  fit measured in days -- `randomForestSRC::pbc`, this package's own canonical
  survival example, among them -- rendered "Survival at 1191 year" for a horizon
  of 1191 days. The titles now read "Survival at 1191" and "time" by default,
  and a new `time_units` argument on both methods restores an explicit unit:
  `time_units = "days"` gives "Survival at 1191 days" and "time (days)". Users
  whose data really is in years should pass `time_units = "years"` to keep the
  word.
```

Do NOT change the `Version:` line. It stays `4.0.0`.

- [ ] **Step 2: Document**

```bash
Rscript -e 'devtools::document()'
```

Expected: `man/plot.gg_variable.Rd` and `man/plot.gg_rfsrc.Rd` gain a `time_units`
entry. `NAMESPACE` is unchanged — the three helpers are dot-prefixed internals with a
bare `@keywords internal` block and no title, so they generate no topic and no export.

- [ ] **Step 3: Lint**

```bash
Rscript -e 'lintr::lint_package()'
```

Expected: no output (0 lints).

- [ ] **Step 4: Record the snapshot state, then run the full suite**

```bash
git status --short tests/testthat/_snaps/
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::test()'
git status --short tests/testthat/_snaps/
```

Expected: exactly two failures, `gg_rfsrc survival no ci` and
`gg_rfsrc survival bootstrap ci`, each reporting a changed SVG. Everything else 0
failures, 0 errors.

**Stop and investigate if** `git status` shows *deleted* `_snaps/` files — that is the
pruning failure mode and means the env vars were dropped. Recover with
`git checkout -- tests/testthat/_snaps/` **only at this point**, before any baseline
has been deliberately regenerated, then rerun with both variables set.

- [ ] **Step 5: Regenerate the two baselines**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'testthat::snapshot_accept("snapshots")'
git status --short tests/testthat/_snaps/
```

Expected: exactly two modified files,
`tests/testthat/_snaps/snapshots/gg-rfsrc-survival-no-ci.svg` and
`tests/testthat/_snaps/snapshots/gg-rfsrc-survival-bootstrap-ci.svg`. No deletions,
no other modifications. If more than two files changed, stop — something outside this
change moved.

- [ ] **Step 6: Confirm the regenerated baselines carry the new label**

```bash
grep -c 'time (years)' tests/testthat/_snaps/snapshots/gg-rfsrc-survival-*.svg
grep -o '>time<' tests/testthat/_snaps/snapshots/gg-rfsrc-survival-no-ci.svg
```

Expected: `0` for both files on the first command; `>time<` found on the second.

- [ ] **Step 7: Re-run the full suite clean**

```bash
NOT_CRAN=true VDIFFR_RUN_TESTS=true Rscript -e 'devtools::test()'
git status --short tests/testthat/_snaps/
```

Expected: 0 failures, 0 errors. `git status` shows only the two intended baseline
modifications.

- [ ] **Step 8: Commit**

```bash
git add NEWS.md man/ NAMESPACE tests/testthat/_snaps/
git commit -m "docs: NEWS and man pages for settable survival time units"
```

- [ ] **Step 9: R CMD check from a clean export**

Build from a `git archive` export, not the working tree: in a worktree `.git` is a
*file*, so `R CMD build`'s VCS exclusion misses it and it lands in the tarball as a
spurious hidden-files NOTE.

```bash
rm -rf "$TMPDIR/ggrf-check" && mkdir -p "$TMPDIR/ggrf-check" && git archive HEAD | tar -x -C "$TMPDIR/ggrf-check"
cd "$TMPDIR/ggrf-check" && R CMD build . && R CMD check --as-cran ggRandomForests_4.0.0.tar.gz
```

Expected: 0 errors, 0 warnings. Compare the NOTE count against `main` — this change
must not add one. Do not pass `--no-manual` or `--no-build-vignettes`.

- [ ] **Step 10: Push and open the PR**

```bash
git push -u origin claude/sweet-williams-2c2479
```

Then open a PR describing the defect, the `time_units` argument, and the two
regenerated baselines. **Stop there — the maintainer merges.**
