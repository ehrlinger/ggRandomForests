% Generated by roxygen2 (4.1.0): do not edit by hand
% Please edit documentation in R/Boston_var.R
\docType{data}
\name{Boston_var}
\alias{Boston_var}
\title{Cached  \code{randomForestSRC::var.select} object for the \code{Boston} Housing data.}
\format{A randomForestSRC::var.select object for regression}
\description{
The \code{Boston_var} data set is constructed from the
\code{randomForestSRC::var.select}
call on the random forest model (\code{randomForestSRC::rfsrc} in \code{\link{Boston_rfsrc}})
for \code{Boston} Housing data in the \code{MASS} package.
}
\details{
Many \code{randomForestSRC} functions are computationally expensive. We use cached objects to
improve the \code{ggRandomForests} diagnostic and example run times (see
 \code{\link{rebuild_cache_datasets}} to rebuild your own copy.)

We build a regression random forest (\code{\link{Boston_rfsrc}})
with the \code{Boston} housing data set in the \code{MASS} package, then run the
\code{randomForestSRC::var.select}
function for minimal depth variabl selection data.

Housing Values in Suburbs of Boston containing 506 rows and 14 columns.
}
\examples{
\dontrun{
## The data was built with the following randomForestSRC commands
data(Boston, package="MASS")
Boston$chas <- as.logical(Boston$chas)
Boston_rfsrc <- rfsrc(medv~., data=Boston, ...)

Boston_var <- var.select(Boston_rfsrc)

# ggRandomForests commands:
gg_dta <- gg_minimal_depth(Boston_var)
plot(gg_dta)

gg_dta <- gg_minimal_vimp(airq_vs)
plot(gg_dta)
}
}
\seealso{
\code{airquality} \code{randomForestSRC::var.select} \code{randomForestSRC::rfsrc}
 \code{\link{Boston_rfsrc}} \code{\link{rebuild_cache_datasets}} \code{\link{gg_minimal_depth}}
 \code{\link{plot.gg_minimal_depth}}  \code{\link{gg_minimal_vimp}}
 \code{\link{plot.gg_minimal_vimp}}
}
\keyword{datasets}

