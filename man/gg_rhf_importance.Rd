% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_rhf_importance.R
\name{gg_rhf_importance}
\alias{gg_rhf_importance}
\alias{gg_rhf_importance.rhf}
\title{Tidy time-localized variable priority from a Random Hazard Forest}
\usage{
gg_rhf_importance(object, ...)

\method{gg_rhf_importance}{rhf}(
  object,
  importance_fit = NULL,
  cache = NULL,
  time.index = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{rhf} object from pkg{randomForestRHF}.}

\item{...}{Additional arguments passed to
\code{\link[randomForestRHF:importance.rhf]{randomForestRHF::importance.rhf()}} when \code{importance_fit} is \code{NULL}.}

\item{importance_fit}{Optional precomputed
\code{\link[randomForestRHF:importance.rhf]{randomForestRHF::importance.rhf()}} result for the same \code{object}. Supply
this object when you have already calculated variable priority. \code{NULL}
(default) calculates it from \code{object}.}

\item{cache}{Optional precomputed \code{\link[randomForestRHF:varpro.cache.rhf]{randomForestRHF::varpro.cache.rhf()}}
result used when \code{importance_fit} is \code{NULL}.}

\item{time.index}{Optional time-grid indices passed to
\code{\link[randomForestRHF:importance.rhf]{randomForestRHF::importance.rhf()}} when \code{importance_fit} is \code{NULL}.}
}
\value{
A \code{data.frame} of class
\code{c("gg_rhf_importance", "data.frame")} with columns:
\describe{
\item{variable}{Variable name, ordered by \code{q90} priority for plotting.}
\item{time_window}{Upstream time-window label.}
\item{time}{Evaluation time at the end of the window.}
\item{time_index}{Index of \code{time} on the RHF time grid.}
\item{start, stop, midpoint}{Window boundaries and midpoint.}
\item{n_risk}{Number of observations at risk.}
\item{n_rules}{Number of rules contributing to the window.}
\item{priority}{RHF variable-priority score.}
}
A \code{provenance} attribute records the source forest, upstream settings,
whether \code{importance_fit} was supplied, and the installed
pkg{randomForestRHF} version.
}
\description{
Extracts the variable-priority matrix from
\code{\link[randomForestRHF:importance.rhf]{randomForestRHF::importance.rhf()}} into a tidy data frame, one row per
variable and time window. The score measures how much the fitted integrated
hazard changes when rules involving a variable are released.
}
\details{
Variable priority is time-localized. A large value means that releasing
rules involving that variable changed the log integrated hazard more in
that window. It is a ranking score, not a z-score, and this function does
not apply a significance cutoff.

Calculating the upstream result can be expensive. For an analysis you will
revisit, calculate \code{importance_fit} once and supply it here. The extractor
accepts \code{cache}, \code{time.index}, and additional calculation arguments only
when \code{importance_fit} is \code{NULL}.

Variables are ordered for plotting by their 90th percentile (\code{q90})
priority across windows. This changes the factor levels, but does not change
the upstream row order or priority values.
}
\examples{
\donttest{
if (requireNamespace("randomForestRHF", quietly = TRUE)) {
  data(pbc, package = "randomForestSRC")
  d <- randomForestRHF::convert.counting(
    survival::Surv(days, status) ~ ., na.omit(pbc))
  o <- randomForestRHF::rhf(
    "Surv(id, start, stop, event) ~ .", d, ntree = 30)

  priority_fit <- randomForestRHF::importance.rhf(o)
  priority <- gg_rhf_importance(o, importance_fit = priority_fit)
  plot(priority)
}
}

}
\references{
Ishwaran H, Hsich EM, Kogalur UB, Lee DKK (2026). Random Hazard Forests.
arXiv:2608.21597. \doi{10.48550/arXiv.2608.21597}.

Ishwaran H, Kogalur UB (2026). \emph{randomForestRHF: Random Hazard
Forests}. R package version 1.0.1.
\url{https://CRAN.R-project.org/package=randomForestRHF}.
}
\seealso{
\code{\link[=plot.gg_rhf_importance]{plot.gg_rhf_importance()}}, \code{\link[randomForestRHF:importance.rhf]{randomForestRHF::importance.rhf()}}
}
