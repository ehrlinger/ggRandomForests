% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.gg_rhf.R
\name{plot.gg_rhf}
\alias{plot.gg_rhf}
\title{Plot Random Hazard Forest hazard / cumulative-hazard curves}
\usage{
\method{plot}{gg_rhf}(x, idx = NULL, hazard.only = TRUE, ...)
}
\arguments{
\item{x}{A \code{gg_rhf} object from \code{\link[=gg_rhf]{gg_rhf()}}.}

\item{idx}{Integer vector of case ids (matched against the \code{id} column) to
draw. \code{NULL} (default) draws every case.}

\item{hazard.only}{Logical; \code{TRUE} (default) plots the hazard, \code{FALSE}
plots the cumulative hazard.}

\item{...}{Not currently used.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws case-specific ensemble curves from a \code{\link[=gg_rhf]{gg_rhf()}} object: hazard
(default) or cumulative hazard, one line per case selected by \code{idx}.
}
\examples{
\donttest{
if (requireNamespace("randomForestRHF", quietly = TRUE)) {
  data(pbc, package = "randomForestSRC")
  d <- randomForestRHF::convert.counting(
    survival::Surv(days, status) ~ ., na.omit(pbc))
  o <- randomForestRHF::rhf("Surv(id, start, stop, event) ~ .", d, ntree = 30)
  plot(gg_rhf(o), idx = c(1, 5, 10))
}
}

}
\seealso{
\code{\link[=gg_rhf]{gg_rhf()}}.
}
