% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_rhf.R
\name{gg_rhf}
\alias{gg_rhf}
\alias{gg_rhf.rhf}
\title{Tidy hazard and cumulative-hazard curves from a Random Hazard Forest}
\usage{
gg_rhf(object, ...)

\method{gg_rhf}{rhf}(object, source = c("oob", "inbag"), ...)
}
\arguments{
\item{object}{A fitted \code{rhf} object from \pkg{randomForestRHF}.}

\item{...}{Not currently used.}

\item{source}{Which ensemble estimate to extract: \code{"oob"} (default,
out-of-bag) or \code{"inbag"}. Falls back to the other when the requested one
is absent (e.g. \code{bootstrap = "none"} has no OOB estimate).}
}
\value{
A \code{data.frame} of class \code{c("gg_rhf", "data.frame")} with columns
\code{id}, \code{time}, \code{hazard}, \code{chf}, \code{source}, an integer \code{ntime} attribute
(number of grid points), and a \code{provenance} attribute.
}
\description{
Extracts case-specific ensemble hazard and cumulative-hazard estimates from
a fitted \code{\link[randomForestRHF:rhf]{randomForestRHF::rhf()}} object into a tidy long data frame, one
row per (case, time) pair on the forest's \code{time.interest} grid.
}
\examples{
\donttest{
if (requireNamespace("randomForestRHF", quietly = TRUE)) {
  data(pbc, package = "randomForestSRC")
  d <- randomForestRHF::convert.counting(
    survival::Surv(days, status) ~ ., na.omit(pbc))
  o <- randomForestRHF::rhf("Surv(id, start, stop, event) ~ .", d, ntree = 30)
  gg <- gg_rhf(o)
  plot(gg, idx = c(1, 5, 10))
}
}

}
\seealso{
\code{\link[=plot.gg_rhf]{plot.gg_rhf()}}, \code{\link[randomForestRHF:rhf]{randomForestRHF::rhf()}}
}
