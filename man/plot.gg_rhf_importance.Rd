% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.gg_rhf_importance.R
\name{plot.gg_rhf_importance}
\alias{plot.gg_rhf_importance}
\title{Plot Random Hazard Forest variable priority over time}
\usage{
\method{plot}{gg_rhf_importance}(
  x,
  vars = NULL,
  top_n_union = 15L,
  transform = c("none", "log10"),
  size_cap = 0.99,
  color_cap = 0.99,
  display_note = TRUE,
  labels = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{gg_rhf_importance} object from \code{\link[=gg_rhf_importance]{gg_rhf_importance()}}.}

\item{vars}{Optional nonempty character vector of variables to display.
Unknown names are an error. When supplied, this takes precedence over
\code{top_n_union}.}

\item{top_n_union}{\code{NULL} or one positive integer. When \code{vars} is \code{NULL},
each time window contributes this many leading variables and the plot
displays their union. \code{NULL} displays every variable.}

\item{transform}{Display transformation: \code{"none"} (default) or \code{"log10"},
which uses \code{log10(priority + 1)}. The returned extractor object is never
changed.}

\item{size_cap, color_cap}{One numeric value in \verb{(0, 1]}. Point size and
color are capped at these quantiles of the finite display values. A value
of \code{1} applies no cap.}

\item{display_note}{Logical; if \code{TRUE}, an applied size or color cap is
reported in the caption.}

\item{labels}{Optional variable labels for the variable axis. One of: a
named character vector (\code{c(bili = "Serum bilirubin")}); a labelled data
frame, whose \code{attr(col, "label")} values are read; or a two-column
\code{key}/\code{label} data frame. Variables with no label keep their raw name.
Defaults to \code{NULL} (raw names).}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a point matrix from a \code{\link[=gg_rhf_importance]{gg_rhf_importance()}} object. Each row is a
variable, each column is a time window, and point size and color carry the
time-localized RHF variable-priority score.
}
\details{
Variables retain the global \code{q90} ordering prepared by
\code{\link[=gg_rhf_importance]{gg_rhf_importance()}}, with the highest-ranked variable at the top. Time
windows remain chronological. This follows the variable-priority matrix in
Ishwaran et al. (2026) while returning a ggplot object you can extend.

The transformation and caps affect display values only. The \code{priority}
column in \code{x} remains on the upstream scale. A zero priority is drawn at the
minimum point size; missing priorities are not drawn. If variable filtering
leaves no finite values, the method stops rather than returning an empty
plot.
}
\examples{
\donttest{
if (requireNamespace("randomForestRHF", quietly = TRUE)) {
  data(pbc, package = "randomForestSRC")
  d <- randomForestRHF::convert.counting(
    survival::Surv(days, status) ~ ., na.omit(pbc))
  o <- randomForestRHF::rhf(
    "Surv(id, start, stop, event) ~ .", d, ntree = 30)
  priority_fit <- randomForestRHF::importance.rhf(o)
  priority <- gg_rhf_importance(o, importance_fit = priority_fit)

  plot(priority, top_n_union = 10)
}
}

}
\references{
Ishwaran H, Hsich EM, Kogalur UB, Lee DKK (2026). Random Hazard Forests.
arXiv:2608.21597. \doi{10.48550/arXiv.2608.21597}.
}
\seealso{
\code{\link[=gg_rhf_importance]{gg_rhf_importance()}}, \code{\link[randomForestRHF:dotmatrix.importance.rhf]{randomForestRHF::dotmatrix.importance.rhf()}}
}
